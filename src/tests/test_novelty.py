import numpy as np
from challenge.novelty.pca_index import PCAIndex
from challenge.novelty.mahalanobis import MahalanobisIndex

def test_pca_index_api():
    model = 
    ref =
    try:
        model.fit(ref)
    except NotImplementedError:
        return  # expected until implemented

def test_mahalanobis_api():
    model = MahalanobisIndex()
    ref = 
    try:
        model.fit(ref)
    except NotImplementedError:
        return
